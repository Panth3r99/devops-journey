name: Docker Server Monitor

on: [push]

jobs:
  docker-run:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Build Docker image
        run: docker build -t server-monitor .

      - name: Run Docker container
        run: docker run server-monitor
